<!-- children.indexa.html.erb -->
<!-- storage for children.index potential code -->
<h1> BFFs that you already have: </h1>
	<div class="fourCol_belly_div">
		<table border = "0", style="width=100%">
			<tr>
				<td><u>Child's Name</u></td>
				<td><u>Age</u></td>
				<td><u>Child's School</u></td>
			</tr>
			<tr>
				<% @friends.each do |f| %>
					<% if @playfriends.where(playfriend_id:f) != [] %>
						<td><%= "#{f.fname}  #{f.lname}" %></td>
						<td><%= f.age_years %></td>
						<td><%= f.hobbyteachone %></td>
			</tr>
						<% end %><!--   end if friends include --> 
						<% end %>
			</table>
	</div><!--  close fourCol_belly_div -->
<br>
<br>
</div>
<br>
<!-- ############################### -->
<h1> Choose new friends to make</h1>	
<h4>Your current friends are still on this list as "Drop Friend?". You can add more friends by selecting kids with "Add Friends?" They will be added to your list above.</h4>  
	<div class="fourCol_belly_div">
		<table border = "0", style="width=100%">
			<tr>
				<h3><td><u>Child's Name</u></td>
					<td><u>Age </u></td>
					<td><u>Child's School</u></td>
					<td><u>Add to BFFs list above?</u></td>
				</tr>
				<% @friends.each do |f| %>
				<tr>
					<td><%= "#{f.fname}  #{f.lname}" %></td>
					<td><%= f.age_years %></td>
					<td><%= f.hobbyteachone %></td>
					<td><% if @playfriends.where(playfriend_id:f) != [] %>
						<span>
							<td>
								<% link_to "Drop Friend?", friend_drop_path(playfriend_id:(f)), method: :post %>  
							</td>
						</span>
						<% else %>
						 <span>
						 	<td>
						 		<% link_to "Add to BFFs?", friend_add_path(playfriend_id:(f)), method: :post %> 
						 	</td>
						 </span>
						 <% end %><!--  end if -->
						</tr>
				<% end %><!--  end do -->
			</table>
		<br>
		<br>
		</div> <!-- close belly_div -->
<!-- navigation tabs -->
<br>

